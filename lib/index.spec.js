const testCandidate = require('./index'),
    expect = require('chai').expect;

describe('lib/index ----- ', () => {

    it('should behave ...', () => {
        expect(testCandidate).to.be.not.null;
    });
});
